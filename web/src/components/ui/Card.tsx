// import _Card from "@material-ui/core/Card";
import styled from "styled-components";
import { layout } from "styled-system";

export const Card = styled.div`
  border-radius: 10px;
  box-shadow: 0 10px 28px rgba(0, 0, 0, 0.12), 0 4px 8px rgba(0, 0, 0, 0.04);
  background: #fff;
  ${layout}
`;
